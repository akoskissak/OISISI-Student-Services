<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        xmlns:ToolBar="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
        Icon="https://static.vecteezy.com/system/resources/thumbnails/000/511/962/small/57_Student.jpg"
        mc:Ignorable="d"
        Title="Studentska služba"
        WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown">

    <Window.InputBindings>
        <KeyBinding Key="A" Modifiers="Control" />
        <KeyBinding Key="U" Modifiers="Control" />
        <KeyBinding Key="D" Modifiers="Control" />
    </Window.InputBindings>

    <DockPanel>
        <Menu DockPanel.Dock ="Top">
            <MenuItem Header="File">
                <MenuItem Header="New" Click="Add_Click" >
                    <MenuItem.Icon>
                        <Image Source="https://cdn2.iconfinder.com/data/icons/ios-7-icons/50/plus-512.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Save">
                    <MenuItem.Icon>
                        <Image Source="https://static.vecteezy.com/system/resources/previews/020/936/457/original/save-icon-for-your-website-design-logo-app-ui-free-vector.jpg"/>
                    </MenuItem.Icon>
                 </MenuItem>
                <MenuItem Header="Open">
                    <MenuItem.Icon>
                        <Image Source="https://icons.veryicon.com/png/o/object/material-design-icons-1/open-in-app.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Close">
                    <MenuItem.Icon>
                        <Image Source="https://static.thenounproject.com/png/53235-200.png"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Edit" Click="Update_Click">
                    <MenuItem.Icon>
                        <Image Source="https://cdn-icons-png.flaticon.com/512/7398/7398464.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Delete" Click="Delete_Click">
                    <MenuItem.Icon>
                        <Image Source="https://cdn.iconscout.com/icon/free/png-256/free-delete-736-470378.png"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About">
                    <MenuItem.Icon>
                        <Image Source="https://icons.veryicon.com/png/o/miscellaneous/administration/account-25.png"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        
        <DockPanel DockPanel.Dock="Top">
            <Button Name="AddEntityButton" Click="Add_Click" Height="20" HorizontalAlignment="Left" Margin="5,0,5,0" >
                <Image Source="https://cdn-icons-png.flaticon.com/512/32/32339.png" Height="10" />
            </Button>
            <Button Name="UpdateEntityButton" Click="Update_Click" Height="20" HorizontalAlignment="Left" Margin="0,0,5,0">
                <Image Source="https://logowik.com/content/uploads/images/888_edit.jpg"/>
            </Button>
            <Button Name="DeleteEntityButton" Click="Delete_Click" Height="20" HorizontalAlignment="Left" Margin="0,0,5,0">
                <Image Source="https://cdn-icons-png.flaticon.com/512/3405/3405244.png"/>
            </Button>

            <Button Height="20" DockPanel.Dock="Right" HorizontalAlignment="Right" Margin="0,0,5,0">
                <Image Source="https://icons.veryicon.com/png/o/commerce-shopping/small-icons-with-highlights/search-260.png"/>
            </Button>
            <TextBox Width="150" DockPanel.Dock="Right" HorizontalAlignment="Right" Margin="0,0,5,0"/>
        </DockPanel>
        

        <DockPanel DockPanel.Dock="Bottom">
                <TextBlock Text="Studentska služba"></TextBlock>
                <TextBlock x:Name="DisplayDateTextBlock" Text="" HorizontalAlignment="Right"></TextBlock>
        </DockPanel>

        <TabControl Name="Tabs" DockPanel.Dock="Top">
            <TabItem Name="StudentsTab" Header="Students">
                <DataGrid Height="350"
                              AutoGenerateColumns="False"
                              x:Name="StudentsDataGrid"
                              ItemsSource="{Binding StudentDtos, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedStudent, UpdateSourceTrigger=PropertyChanged}"
                              SelectionMode="Single" IsReadOnly="True"
                              SelectionChanged="StudentsDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Index" Binding="{Binding Index}"/>
                        <DataGridTextColumn Header="Ime" Binding="{Binding Name}"/>
                        <DataGridTextColumn Header="Prezime" Binding="{Binding LastName}"/>
                        <DataGridTextColumn Header="Godina Studija" Binding="{Binding CurrentYearOfStudy}"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                        <DataGridTextColumn Header="Prosek" Binding="{Binding AverageGrade}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Name="ProfessorsTab" Header="Professors">
                <DataGrid Height="350"
                              AutoGenerateColumns="False"
                              x:Name="ProfessorsDataGrid"
                              ItemsSource="{Binding ProfessorDtos, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedProfessor, UpdateSourceTrigger=PropertyChanged}"
                              SelectionMode="Single" IsReadOnly="True"
                              SelectionChanged="ProfessorsDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Lastname" Binding="{Binding Lastname}" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                        <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Name="SubjectsTab" Header="Subjects">
                <DataGrid Height="350"
                              AutoGenerateColumns="False"
                              x:Name="SubjectsDataGrid"
                              ItemsSource="{Binding SubjectDtos, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedSubject, UpdateSourceTrigger=PropertyChanged}"
                              SelectionMode="Single" IsReadOnly="True"
                              SelectionChanged="SubjectDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="SubjectCode" Binding="{Binding SubjectCode}" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Epsb" Binding="{Binding Espb}" />
                        <DataGridTextColumn Header="YearOfStudy" Binding="{Binding YearOfStudy}" />
                        <DataGridTextColumn Header="Semester" Binding="{Binding Semester}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

    </DockPanel>
</Window>
